<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.sogoamobile.weatherapp.presentation.HomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_homeFragment_to_weatherFragment"
            app:destination="@id/weatherFragment" />
    </fragment>
    <fragment
        android:id="@+id/weatherFragment"
        android:name="com.sogoamobile.weatherapp.presentation.WeatherInfoFragment"
        android:label="Weather Info"
        tools:layout="@layout/fragment_weather_info">

        <argument
            android:name="long"
            app:argType="string"
            android:defaultValue="0" />
        <argument
            android:name="lat"
            app:argType="string"
            android:defaultValue="0" />
    </fragment>

</navigation>